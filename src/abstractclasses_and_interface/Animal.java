package abstractclasses_and_interface;

public interface Animal {
	/*
	 * Both the line is same because every variable inside interface is public static
	 * final
	 */
	
	int legs = 4;
	public static final int teeth = 32;
}
